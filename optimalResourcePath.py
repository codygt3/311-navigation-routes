#Given a resource to distribute, returns a path to distribute the resource evenly to each island based on population
def optimalResourcePath(graph, resource):
    return "path"
    
#Using the path given, returns the resulting graph, which should have that resource distributed.
def useOptimalResourcePath(graph, path):
    return "graph"